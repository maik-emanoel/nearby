import { StyleSheet } from 'react-native'

export const s = StyleSheet.create({
  container: {
    maxHeight: 56,
    position: 'absolute',
    zIndex: 1,
    top: 64,
  },
  content: {
    gap: 8,
    paddingHorizontal: 24,
  },
})
